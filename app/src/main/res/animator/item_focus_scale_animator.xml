<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Animação para quando o item está focado -->
    <item android:state_focused="true">
        <set>
            <objectAnimator
                android:propertyName="scaleX"
                android:duration="@android:integer/config_shortAnimTime"
                android:valueTo="1.05"
                android:valueType="floatType"/>
            <objectAnimator
                android:propertyName="scaleY"
                android:duration="@android:integer/config_shortAnimTime"
                android:valueTo="1.05"
                android:valueType="floatType"/>
            <!-- Poderia adicionar elevação (translationZ) também se os itens fossem CardViews ou tivessem elevação -->
            <!-- <objectAnimator
                android:propertyName="translationZ"
                android:duration="@android:integer/config_shortAnimTime"
                android:valueTo="8dp"
                android:valueType="floatType"/> -->
        </set>
    </item>
    <!-- Animação para quando o item não está focado (retorna ao normal) -->
    <item android:state_focused="false">
        <set>
            <objectAnimator
                android:propertyName="scaleX"
                android:duration="@android:integer/config_shortAnimTime"
                android:valueTo="1.0"
                android:valueType="floatType"/>
            <objectAnimator
                android:propertyName="scaleY"
                android:duration="@android:integer/config_shortAnimTime"
                android:valueTo="1.0"
                android:valueType="floatType"/>
            <!-- <objectAnimator
                android:propertyName="translationZ"
                android:duration="@android:integer/config_shortAnimTime"
                android:valueTo="0dp"
                android:valueType="floatType"/> -->
        </set>
    </item>
</selector>
